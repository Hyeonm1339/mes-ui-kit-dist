import type { ToasterProps } from 'sonner';
interface AppToasterProps {
    position?: ToasterProps['position'];
}
export declare const AppToaster: ({ position }: AppToasterProps) => import("react/jsx-runtime").JSX.Element;
export {};
